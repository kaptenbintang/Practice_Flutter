import 'package:flutter/foundation.dart' show immutable;

// field name at Location table firestore

@immutable
class LocationKey {
  static const type = 'type';
  // static const practionerSchedule = 'practionerSchedule';

  const LocationKey._();
}
